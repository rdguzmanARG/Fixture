{
  "name": "fixture",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build": "yarn install --production=false && yarn workspace fixture-server prisma:generate && yarn workspace fixture-server prisma:migrate:deploy && yarn workspace fixture-client build && yarn workspace fixture-server build",
    "dev:server": "yarn workspace fixture-server dev",
    "dev:client": "yarn workspace fixture-client dev",
    "seed": "yarn workspace fixture-server db:seed"
  }
}